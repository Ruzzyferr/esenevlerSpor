spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/esenevlerspor
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: update
      properties:
        hibernate:
          jdbc:
            time_zone: UTC
    show-sql: true
  mail:
    host: smtp.gmail.com
    port: 587
    username: ruzgaremirofficial@gmail.com
    password: pvsvzxpkyxwpjarb
    properties:
      mail:
        smtp:
          auth: true
          starttls.enable: true

